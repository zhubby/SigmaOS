{
  "name": "@sigmaos/terminal-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsup src/index.ts --format esm --platform node --target node22 --out-dir dist"
  },
  "dependencies": {
    "@sigmaos/shared": "0.1.0",
    "node-pty": "^1.1.0"
  }
}
