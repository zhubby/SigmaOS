{
  "name": "@sigmaos/photo-worker",
  "version": "0.6.0",
  "type": "module",
  "exports": {
    ".": "./src/processor.ts"
  },
  "types": "./src/processor.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "cd ../.. && tsup apps/photo-worker/src/index.ts --format esm --platform node --target node22 --external=better-sqlite3 --external=sharp --out-dir apps/photo-worker/dist"
  },
  "dependencies": {
    "@sigmaos/db": "0.6.0",
    "@sigmaos/nas-tools": "0.6.0",
    "@sigmaos/shared": "0.6.0",
    "exifr": "^7.1.3",
    "sharp": "^0.35.4"
  }
}
