{
  "name": "@sigmaos/worker",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/processor.ts"
  },
  "types": "./src/processor.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "cd ../.. && tsup apps/worker/src/index.ts --format esm --platform node --target node22 --external=better-sqlite3 --external=@earendil-works/pi-coding-agent --out-dir apps/worker/dist"
  },
  "dependencies": {
    "@sigmaos/agent": "0.1.0",
    "@sigmaos/db": "0.1.0",
    "@sigmaos/shared": "0.1.0"
  }
}
